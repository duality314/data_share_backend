data_share_backend/
├── app.py                 # Flask 应用入口，初始化并注册蓝图，端口/ip
├── config.py              # 配置类（数据库、JWT、跨域等），已合并原 db.py
├── requirements.txt       # Python 依赖
├── docker-compose.yml     # Docker 编排配置
├── README.md              # 项目说明
├── sqltest.md             # SQL 测试文档
│
├── models/                # 数据模型
│   ├── user.py            # 用户表（数据市场）
│   ├── dataset.py         # 数据集表（数据市场）
│   ├── share.py           # 共享记录表
│   ├── download_log.py    # 下载日志表（todo:共识协议）
│   └── dataset_permission.py  # 数据集权限表
│
├── routes/                # API 路由层
│   ├── auth_routes.py     # 认证相关
│   ├── dataset_routes.py  # 数据集相关
│   └── share_routes.py    # 共享相关
│
├── controllers/           # 业务逻辑层
│   ├── auth_controller.py
│   ├── dataset_controller.py
│   └── share_controller.py
│
├── schemas/               # 请求/响应数据校验
│   ├── auth_schema.py
│   ├── dataset_schema.py
│   └── share_schema.py
│
├── utils/                 # 工具函数
│   └── preview.py         # 文件预览
│
├── scripts/               # 脚本
│   └── run_smoke.py       # 冒烟测试
│
├── tests/                 # 测试
│   └── smoke_test.py
│
└── uploads/               # 上传文件目录